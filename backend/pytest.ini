[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: exercises non-functional/performance requirements (e.g. NFR-05); excluded by default, run explicitly with `-m slow`
addopts = -m "not slow"
